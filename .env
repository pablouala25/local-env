# DynamoDB Local
DYNAMODB_PORT=8000
DYNAMODB_CONTAINER_NAME=dynamodb-local
DYNAMODB_DATA_PATH=/home/dynamodblocal/data                        

# DynamoDB Admin
DYNAMODB_ADMIN_PORT=8001
DYNAMODB_ADMIN_CONTAINER_NAME=dynamodb-admin
DYNAMO_ENDPOINT=http://dynamodb-local:8000                          

# Red interna de Docker
APP_NETWORK=app-network                                            

# Tabla DynamoDB usada por tu Lambda
DYNAMODB_TABLE_NAME=my_table                                       

# Credenciales AWS dummy (no usadas por DynamoDB Local, pero requeridas por el SDK)
AWS_REGION=us-east-1         # AWS SDK v2 y dynamodb-admin requieren la región :contentReference[oaicite:0]{index=0}
AWS_ACCESS_KEY_ID=dummy      # AWS CLI y SDK usan esta env var para el access key :contentReference[oaicite:1]{index=1}
AWS_SECRET_ACCESS_KEY=dummy  # AWS CLI y SDK usan esta env var para el secret key :contentReference[oaicite:2]{index=2}
